#!/bin/bash

get_pts (){
  IN=$(wmctrl -l | awk "{print \$4}" | grep "URXVTEVAL")
  arrIN=(${IN//-/ })
  PTS=${arrIN[1]}
}
get_pts
if [ "$PTS" = "" ]
then
  urxvt -e zsh -is eval "cd $PWD ; trap 'echo -ne \"\e]0;URXVTEVAL-\$TTY\007\"' DEBUG" &
  sleep 0.5
  get_pts
fi
ttyecho -n $PTS $@
